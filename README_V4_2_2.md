# JKR Investments v4.2.2 — Component Contrast Patch

This patch fixes the remaining light-theme visibility issues in:

- Technicals search and filter strip
- Results metric and pillar cards
- News & Corporate Announcements “Why it matters” and “What to verify next” panels
- Placeholder, muted text, borders, and active filter contrast

It also bumps the CSS/service-worker cache version so installed PWAs receive the update reliably.

No Supabase, broker, market-data, or JavaScript logic changes are included.
