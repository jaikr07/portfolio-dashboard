import {mountShell} from './shell.js';import {loadCore,aggregateHoldings,loadAnnouncements,saveManualAnnouncement} from './data-service.js';import {esc,impactClass,today,safeUrl} from './utils.js';import {updateModeBadge,bindModal} from './common.js';
mountShell('announcements','News & Corporate Announcements');const root=document.getElementById('pageContent');
async function run(){const [core,ann]=await Promise.all([loadCore(),loadAnnouncements()]);const active=new Set(aggregateHoldings(core.instruments,core.transactions).map(x=>x.symbol));const rows=ann.filter(x=>active.has(x.symbol)).sort((a,b)=>String(b.published_at).localeCompare(String(a.published_at)));root.innerHTML=`<div class="hero"><div><h2>Material developments</h2><p>Acquisitions, orders, capacity additions, demergers, dividends, management changes and other developments, summarized with an evidence-based impact score from −5 to +5.</p></div><button class="btn primary" id="openAnn">+ Add manual announcement</button></div><div class="notice warning">Impact scores describe likely business direction and materiality, not predicted share-price movement. Always open the original source before acting.</div><div class="card"><div class="toolbar"><input class="input" id="annSearch" placeholder="Search title or symbol"><select class="input" id="annImpact"><option value="">All impacts</option><option value="positive">Positive</option><option value="negative">Negative</option><option value="neutral">Neutral</option></select></div><div id="annList">${render(rows)}</div></div><div class="modal-backdrop" id="annModal"><div class="modal"><h3>Add a verified announcement</h3><form id="annForm" class="form-grid"><div class="field"><label>Symbol</label><input class="input" name="symbol" required></div><div class="field"><label>Date</label><input type="date" class="input" name="published_at" value="${today()}" required></div><div class="field full"><label>Title</label><input class="input" name="title" required></div><div class="field"><label>Category</label><select class="input" name="category"><option>Order win</option><option>Acquisition</option><option>Demerger</option><option>Results</option><option>Capacity expansion</option><option>Dividend / bonus</option><option>Governance</option><option>Other</option></select></div><div class="field"><label>Impact score (−5 to +5)</label><input type="number" min="-5" max="5" class="input" name="impact_score" value="0"></div><div class="field full"><label>Summary / reasoning</label><textarea class="input" rows="4" name="summary" required></textarea></div><div class="field full"><label>Source URL</label><input class="input" type="url" name="source_url"></div><div class="full" style="display:flex;justify-content:flex-end;gap:8px"><button type="button" class="btn" id="closeAnn">Cancel</button><button class="btn primary">Save</button></div></form></div></div>`;
 bindModal('annModal','openAnn',['closeAnn']);document.getElementById('annForm').addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target));f.symbol=f.symbol.toUpperCase();f.impact_score=Number(f.impact_score);f.impact_label=f.impact_score>=2?'Bullish':f.impact_score<=-2?'Bearish':'Neutral';f.confidence='manual';await saveManualAnnouncement(f);location.reload()});
 const apply=()=>{const q=document.getElementById('annSearch').value.toLowerCase(),imp=document.getElementById('annImpact').value;document.getElementById('annList').innerHTML=render(rows.filter(x=>(!q||`${x.symbol} ${x.title} ${x.summary}`.toLowerCase().includes(q))&&(!imp||impactClass(x.impact_score)===imp)))};document.getElementById('annSearch').addEventListener('input',apply);document.getElementById('annImpact').addEventListener('change',apply);await updateModeBadge(rows.map(x=>x.fetched_at).filter(Boolean).sort().at(-1))}
function render(rows){return rows.map(a=>`<article class="announcement"><div><span class="badge neutral">${esc(a.symbol)}</span><div class="announcement-meta" style="margin-top:8px">${esc(String(a.published_at||'').slice(0,10))}</div></div><div><h4>${safeUrl(a.source_url)?`<a href="${esc(safeUrl(a.source_url))}" target="_blank" rel="noopener noreferrer">${esc(a.title)} ↗</a>`:esc(a.title)}</h4><p>${esc(a.summary||'Summary unavailable.')}</p><div class="announcement-meta" style="margin-top:8px">${esc(a.category||'Other')} • ${esc(a.source||'Manual')} • confidence ${esc(a.confidence||'low')}</div></div><div><div class="impact-score ${impactClass(a.impact_score)}">${Number(a.impact_score||0)>0?'+':''}${Number(a.impact_score||0)}</div><span class="badge ${impactClass(a.impact_score)}">${esc(a.impact_label||'Neutral')}</span></div></article>`).join('')||'<div class="empty">No announcements found.</div>'}
run().catch(e=>root.innerHTML=`<div class="notice warning">${esc(e.message)}</div>`);
