import {mountShell} from './shell.js';import {isCloudConfigured,session,signIn,signOut,importBrokerCsv,exportLocal,importLocal,resetLocal,loadCore,upsertInstrument} from './data-service.js';import {download,esc} from './utils.js';import {updateModeBadge} from './common.js';
mountShell('settings','Settings & Data Import');const root=document.getElementById('pageContent');
async function run(){const sess=await session(),core=await loadCore();root.innerHTML=`<div class="hero"><div><h2>Setup and privacy</h2><p>Import your broker CSV, enable protected cloud sync, export backups and manage the source-symbol mapping.</p></div></div><div class="grid two"><div class="card"><h3>1. Import your holdings CSV</h3><p class="muted">Supports the uploaded format with Instrument, Qty. and Avg. cost. Importing replaces earlier imported opening balances but keeps transactions you entered manually.</p><input type="file" id="csvFile" accept=".csv" class="input" style="width:100%;margin:12px 0"><button class="btn primary" id="importCsv">Import CSV</button><div id="csvMsg" class="kpi-sub"></div></div><div class="card"><h3>2. Cloud sync ${isCloudConfigured()?'<span class="badge positive">Configured</span>':'<span class="badge warning">Not configured</span>'}</h3>${sess?`<p>Signed in as <strong>${esc(sess.user.email)}</strong>.</p><button class="btn danger" id="logout">Sign out</button>`:`<p class="muted">Optional. Add your Supabase project URL and publishable key in <code>assets/js/config.js</code>, then sign in below.</p><form id="loginForm" class="form-grid"><div class="field full"><label>Email</label><input type="email" name="email" class="input" required></div><div class="field full"><label>Password</label><input type="password" name="password" class="input" required></div><div class="full"><button class="btn primary">Sign in</button></div></form>`}</div></div><div class="grid two" style="margin-top:16px"><div class="card"><h3>Backup local data</h3><div class="toolbar"><button class="btn" id="exportBtn">Export JSON backup</button><label class="btn">Import JSON<input type="file" id="jsonFile" accept=".json" hidden></label><button class="btn danger" id="resetBtn">Reset local data</button></div></div><div class="card"><h3>Current data status</h3><div class="signal-list"><div class="signal-item"><div><strong>${core.instruments.length} instruments</strong><p>Symbol and data-provider mappings</p></div></div><div class="signal-item"><div><strong>${core.transactions.length} transactions</strong><p>Opening balances, buys, sells and adjustments</p></div></div></div></div></div><div class="card" style="margin-top:16px"><h3>Instrument and market-data mappings</h3><p class="muted">Edit a Yahoo symbol here when an SME, BE-series, renamed or demerged company is not updating correctly.</p><div class="table-wrap"><table class="data-table"><thead><tr><th>Portfolio symbol</th><th>Yahoo symbol</th><th>Company name</th><th>Exchange</th><th></th></tr></thead><tbody>${core.instruments.map(x=>`<tr><td><span class="symbol">${esc(x.symbol)}</span></td><td><input class="input map-yahoo" data-symbol="${esc(x.symbol)}" value="${esc(x.yahoo_symbol||'')}"></td><td><input class="input map-name" data-symbol="${esc(x.symbol)}" value="${esc(x.name||x.symbol)}"></td><td><input class="input map-exchange" data-symbol="${esc(x.symbol)}" value="${esc(x.exchange||'NSE')}"></td><td><button class="btn small save-map" data-symbol="${esc(x.symbol)}">Save</button></td></tr>`).join('')||'<tr><td colspan="5" class="empty">Import a holdings CSV to create mappings.</td></tr>'}</tbody></table></div></div><div class="card" style="margin-top:16px"><h3>One-time cloud setup</h3><div class="setup-steps"><div class="setup-step"><h4>Create a Supabase project</h4><p>Open the SQL editor and run <code>supabase/schema.sql</code>.</p></div><div class="setup-step"><h4>Create your login</h4><p>In Supabase Authentication, create one user with your email and password. Keep public sign-up disabled if this is a personal dashboard.</p></div><div class="setup-step"><h4>Add two frontend values</h4><p>Copy the project URL and publishable/anon key into <code>assets/js/config.js</code>. The publishable key is safe in the browser only because Row Level Security restricts every row to your authenticated user.</p></div><div class="setup-step"><h4>Add GitHub Actions secrets</h4><p>Add <code>SUPABASE_URL</code>, <code>SUPABASE_SERVICE_ROLE_KEY</code>, and optionally <code>OPENAI_API_KEY</code>. The service-role key must never be placed in frontend code.</p></div></div></div>`;
 document.getElementById('importCsv').addEventListener('click',async()=>{const file=document.getElementById('csvFile').files[0];if(!file)return alert('Choose a CSV file first.');const result=await importBrokerCsv(await file.text());document.getElementById('csvMsg').textContent=`Imported ${result.count} holdings. Refreshing…`;setTimeout(()=>location.reload(),600)});
 document.getElementById('exportBtn').addEventListener('click',()=>download('portfolio-backup.json',exportLocal()));document.getElementById('jsonFile').addEventListener('change',async e=>{importLocal(await e.target.files[0].text());location.reload()});document.getElementById('resetBtn').addEventListener('click',()=>{if(confirm('Delete all browser-only portfolio data?')){resetLocal();location.reload()}});
 document.querySelectorAll('.save-map').forEach(btn=>btn.addEventListener('click',async()=>{const symbol=btn.dataset.symbol;const pick=(cls)=>document.querySelector(`${cls}[data-symbol="${CSS.escape(symbol)}"]`).value;await upsertInstrument({symbol,yahoo_symbol:pick('.map-yahoo'),name:pick('.map-name'),exchange:pick('.map-exchange'),active:true});btn.textContent='Saved';setTimeout(()=>btn.textContent='Save',900)}));document.getElementById('loginForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target));try{await signIn(f.email,f.password);location.reload()}catch(err){alert(err.message)}});document.getElementById('logout')?.addEventListener('click',async()=>{await signOut();location.reload()});await updateModeBadge()}
run().catch(e=>root.innerHTML=`<div class="notice warning">${esc(e.message)}</div>`);
