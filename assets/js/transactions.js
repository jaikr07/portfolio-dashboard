import {mountShell} from './shell.js';import {loadCore,aggregateHoldings,addTransaction,upsertInstrument,deleteTransaction} from './data-service.js';import {fmtMoney,fmtNum,esc,today} from './utils.js';import {updateModeBadge,bindModal} from './common.js';
mountShell('transactions','Transactions & Accumulation');const root=document.getElementById('pageContent');
async function run(){const core=await loadCore(),holdings=aggregateHoldings(core.instruments,core.transactions);root.innerHTML=`<div class="hero"><div><h2>Buy, sell and adjustment ledger</h2><p>Every accumulation or disposal updates the current quantity and weighted average cost.</p></div><button class="btn primary" id="openTx">+ New transaction</button></div><div class="grid kpis"><div class="card"><div class="kpi-label">Transactions</div><div class="kpi-value">${core.transactions.length}</div></div><div class="card"><div class="kpi-label">Active positions</div><div class="kpi-value">${holdings.length}</div></div><div class="card"><div class="kpi-label">Total cost base</div><div class="kpi-value money">${fmtMoney(holdings.reduce((s,x)=>s+x.totalCost,0))}</div></div><div class="card"><div class="kpi-label">Realised P&amp;L</div><div class="kpi-value money">${fmtMoney(holdings.reduce((s,x)=>s+x.realizedPnl,0))}</div></div></div><div class="card"><div class="table-wrap"><table class="data-table"><thead><tr><th>Date</th><th>Symbol</th><th>Type</th><th>Quantity</th><th>Price</th><th>Fees</th><th>Notes</th><th></th></tr></thead><tbody>${[...core.transactions].sort((a,b)=>String(b.trade_date).localeCompare(String(a.trade_date))).map(t=>`<tr><td style="text-align:left">${esc(t.trade_date)}</td><td><span class="symbol">${esc(t.symbol)}</span></td><td><span class="badge ${t.transaction_type==='sell'?'negative':'positive'}">${esc(t.transaction_type)}</span></td><td>${fmtNum(t.quantity,3)}</td><td>${fmtMoney(t.price,2)}</td><td>${fmtMoney(t.fees,2)}</td><td style="text-align:left">${esc(t.notes||'')}</td><td><button class="btn danger small del" data-id="${esc(t.id)}">Delete</button></td></tr>`).join('')||'<tr><td colspan="8" class="empty">No transactions yet.</td></tr>'}</tbody></table></div></div><div class="modal-backdrop" id="txModal"><div class="modal"><h3>Add transaction</h3><form id="txForm" class="form-grid"><div class="field"><label>Symbol</label><input class="input" name="symbol" list="symbols" required><datalist id="symbols">${core.instruments.map(x=>`<option value="${esc(x.symbol)}">`).join('')}</datalist></div><div class="field"><label>Yahoo symbol mapping</label><input class="input" name="yahoo_symbol" placeholder="e.g. RELIANCE.NS"></div><div class="field"><label>Transaction type</label><select class="input" name="transaction_type"><option value="buy">Buy</option><option value="sell">Sell</option><option value="bonus">Bonus</option><option value="split">Split adjustment</option><option value="adjustment">Other adjustment</option></select></div><div class="field"><label>Trade date</label><input type="date" class="input" name="trade_date" value="${today()}" required></div><div class="field"><label>Quantity</label><input type="number" step="any" min="0" class="input" name="quantity" required></div><div class="field"><label>Price per share</label><input type="number" step="any" min="0" class="input" name="price" value="0"></div><div class="field"><label>Fees / taxes</label><input type="number" step="any" min="0" class="input" name="fees" value="0"></div><div class="field full"><label>Notes</label><textarea class="input" name="notes" rows="3"></textarea></div><div class="full" style="display:flex;justify-content:flex-end;gap:8px"><button type="button" class="btn" id="closeTx">Cancel</button><button class="btn primary">Save transaction</button></div></form></div></div>`;
 bindModal('txModal','openTx',['closeTx']);document.getElementById('txForm').addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target));const sym=f.symbol.trim().toUpperCase();if(!core.instruments.some(x=>x.symbol===sym))await upsertInstrument({symbol:sym,yahoo_symbol:f.yahoo_symbol||`${sym.replace(/-(BE|SM|BZ|BL)$/,'')}.NS`,name:sym,exchange:'NSE'});delete f.yahoo_symbol;await addTransaction(f);location.reload()});document.querySelectorAll('.del').forEach(b=>b.addEventListener('click',async()=>{if(confirm('Delete this transaction?')){await deleteTransaction(b.dataset.id);location.reload()}}));await updateModeBadge()}
run().catch(e=>root.innerHTML=`<div class="notice warning">${esc(e.message)}</div>`);
